<!DOCTYPE html>
<html>
  <%- include('partials/head'); -%>
  <body>
    <%- include('partials/topnav'); -%>
    <div class="container">
        <h1 class="text-center"> 
        <a href="#" class="btn btn-secondary">Update category info</a>
        <a href="#" class="btn btn-secondary">Update category image</a>
        <a href="#" class="btn btn-secondary">Delete category</a></h1>
    </div>
    <div class="container-sm">
        <div class="card my-4 mx-auto">
            <img src=<%= category_details.img %> style="max-height:20rem;object-fit:scale-down;" class="card-img-top" alt=<%= category_details.name %>>
            <div class="card-body">
                  <h5 class="card-title"><%= category_details.name %></h5>
            </div>
        </div>
    </div>
  </body>